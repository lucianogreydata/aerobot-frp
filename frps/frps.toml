bindPort = 7000
auth.method = "token"
auth.token = "poc-cambia-este-token"

webServer.addr = "0.0.0.0"        # <-- cambiá esto
webServer.port = 7500
webServer.user = "admin"
webServer.password = "password1@"